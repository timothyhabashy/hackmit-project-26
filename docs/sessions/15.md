# Session 15 — Case detail, evidence inspection, and human correction

## Goal
Complete Case Detail: payment/invoices, source-document viewer, allocations
with validator findings, and the three-step controller form (save, apply,
propose). Do not start Session 16 or consume H01–H20.

## Completed changes
- Case Detail replaces the Session 14 shell. Payment, candidate invoices,
  evidence, decision, saved corrections, and the controller form are on one
  page.
- `Open document` calls `open_evidence`. `Save correction`, `Apply corrected
  resolution`, and `Propose reusable lesson` call the existing services.
  Apply is bound to the correction ID and uses `correction-apply:<id>`.
  Propose is LIVE-only; missing live setup disables it and names the
  variables. `Use example correction` prefills the Harbor teaching text and
  does not submit.
- Decimal cash/fee fields go through `parse_decimal_cents` (integer cents;
  no browser float). Arithmetic preview is shown before apply. A second
  apply cannot post funds: the button disables after the payment is applied,
  and the same idempotency key replays.

T03 in `WS-TEACH-001`: case `CASE-1CFA9FEE848F`, payment `PAY-201` applied,
invoice `INV-1042` outstanding `0`, remittance `DOC-R201`, fee notice
`DOC-F201`. Teaching correction `COR-bd383d0aae4342c08c9c4170618b527f` is
applied as `APP-44b27f710fba4a67b8c06faff43b28d1` (cash `996500`, fee
`3500`, actor `demo_controller`). No draft lesson exists.

## Actual files changed
- `src/precedent/ui.py`, `src/precedent/services.py`, `src/precedent/models.py`
- `src/precedent/db.py`, `src/precedent/learning.py`
- `tests/test_app.py`, `tests/test_models.py`
- `README.md`, `docs/BUILD_STATE.md`, `docs/sessions/15.md`

## Commands and outcomes
All from repo root with `.venv` (CPython 3.11.14).

| Command | Outcome |
|---|---|
| AppTest: T03 open docs, example, save, apply, nav, replay key | PASS. INV-1042 → `0`; `REPLAYED` same application |
| Regression including `tests/test_app.py` | PASS. 221 passed in 5.39s |
| `python -m ruff check src tests app.py` / `ruff format --check` | PASS |
| `precedent preflight --offline` | PASS exit 0. Fixtures `READY` |
| `streamlit run app.py --server.address 127.0.0.1` | PASS. `http://127.0.0.1:8501` |
| Browser: T03, open `DOC-F201`, apply saved teaching correction | PASS. `APP-44b27f710fba4a67b8c06faff43b28d1` |
| Browser refresh → reopen T03 | PASS. Allocations, correction, Apply disabled |
| Browser Propose | Disabled. Names the three missing live variables |

## What was not checked
- No live Anthropic investigate or lesson compile from the UI or CLI.
- Learning Results reports/activation (Session 16).
- Watchdog reload; Streamlit was restarted after UI edits.
- Held-out packages were not opened.

## Unresolved issues
- Live demo blocked until a real `ANTHROPIC_API_KEY`, a provider-verified
  `PRECEDENT_MODEL`, and `PRECEDENT_ENABLE_LIVE=true` exist. Then Propose on
  the eligible T03 correction can be a live compile. Do not substitute
  `ScriptedProvider` for that check.
- No precedent row / ACTIVE memory yet, because live propose did not run.
- Git is not initialized; source version is a behavior-file hash.
- Do not run held-out until Session 18 after functional freeze.

## Deviations
- D001–D004 unchanged.
- Case Detail uses the existing `CaseSnapshot` invoice set (Harbor invoices
  sharing remittance customer), not a new candidate-filter service.
- AppTest cannot click a disabled Apply control; replay is proven with the
  same `correction-apply:<id>` key through `apply_correction`.

## Next-session prerequisites
- Activate `.venv` (`source .venv/bin/activate`).
- Read spec Session 16 plus I4–I6, G4, and H7–H8.
- Build Learning Results only. T03 is already applied; there is still no
  draft lesson until a live compile.
- Do not start Session 17, consume H01–H20, or treat a scripted draft as a
  live lesson test.

## Next chat prompt

```text
Implement Session 16 from docs/PRECEDENT_BUILD_SPEC.md
Read @AGENTS.md, @docs/BUILD_STATE.md, @docs/sessions/15.md, the reference
sections listed in Session 16 (I4–I6; G4; H7–H8), and the actual files you
will edit. Inspect the repository before assuming a prerequisite is done.
Implement only Session 16. Preserve the contracts and existing working
behavior. Open candidate test rows and activate a passing version; compare
report table against raw JSON; verify page refresh makes no network calls.
Add/run a meaningful UI test in tests/test_app.py if AppTest supports the
flow. Replace placeholder IDs with actual generated IDs from the operator
manifest. Use T03 IDs (case CASE-1CFA9FEE848F, payment PAY-201, remittance
DOC-R201, fee notice DOC-F201, invoice INV-1042), application
APP-44b27f710fba4a67b8c06faff43b28d1, and teaching correction
COR-bd383d0aae4342c08c9c4170618b527f. Update docs/BUILD_STATE.md and write
docs/sessions/16.md with results, limitations, and the next-session prompt.
If something is partially complete, resume it; do not rebuild it from
scratch. Do not start Session 17. Live API remains unavailable until
ANTHROPIC_API_KEY, PRECEDENT_MODEL, and PRECEDENT_ENABLE_LIVE=true are
configured; do not substitute mock behavior for that live check. Do not
consume H01–H20.
```
