# Session 14 — Streamlit shell and work queue

## Goal
Launch a restrained Streamlit shell with Work Queue complete: seeded cases,
status counts from SQLite, select/open, LIVE investigate behind an explicit
button, and rerender-safe results. Do not start Session 15 or consume H01–H20.

## Completed changes
- `app.py` renders `precedent.ui`. Theme: light teal in `.streamlit/config.toml`.
- Work Queue lists workspaces and cases from `list_cases` / `queue_counts`.
  Table columns: case ID, payer label, amount, date, status, latest result.
  Counts: open, resolved, needs review, technical errors, persisted runs.
- `Open case` sets `pending_page` then reruns (does not assign `nav_page`
  after the navigation radio exists). `Run agent on selected case` calls
  `investigate(..., mode=LIVE)` only in the button handler. Missing live
  setup disables Run and names `PRECEDENT_ENABLE_LIVE`, `ANTHROPIC_API_KEY`,
  and `PRECEDENT_MODEL`. ScriptedProvider is not used on that path.
- Durable results reload from SQLite. `remembered_workspace_id` /
  `remembered_case_id` restore selection when those widgets are not on the
  page. Case Detail and Learning Results are shells.

T03 IDs still in `WS-TEACH-001`: case `CASE-1CFA9FEE848F`, payment `PAY-201`,
remittance `DOC-R201`, fee notice `DOC-F201`, invoice `INV-1042` outstanding
`1000000`. Teaching correction
`COR-bd383d0aae4342c08c9c4170618b527f` remains eligible and unapplied.
Human run `RUN-031d7c3aef4549cdb1af4c86d11cf2ce` is not `latest_run_id`.

## Actual files changed
- `app.py`, `src/precedent/ui.py`, `.streamlit/config.toml`
- `src/precedent/config.py`, `src/precedent/db.py`, `src/precedent/services.py`
- `tests/test_app.py`, `tests/test_services.py`
- `README.md`, `docs/BUILD_STATE.md`, `docs/sessions/14.md`

## Commands and outcomes
All from repo root with `.venv` (CPython 3.11.14).

| Command | Outcome |
|---|---|
| `python -m pytest -q tests/test_app.py tests/test_services.py` | PASS. 6 passed in 0.59s |
| AppTest: open T01, back to queue, one LIVE-gated investigate | PASS. Count stayed 1 after nav |
| AppTest: missing live vars keep queue; Run disabled; no investigate | PASS |
| `streamlit run app.py --server.address 127.0.0.1` | PASS. `http://127.0.0.1:8502` |
| Browser: `WS-TEACH-001`, T03 `$9,965.00`, Open case | PASS. Correction ID shown |
| Browser: Learning Results, back to queue | PASS. T03 still selected; case runs 1 |
| Browser live Run | Disabled. Names the three missing variables |
| Regression including `tests/test_app.py` | PASS. 219 passed in 5.10s |
| `python -m ruff check src tests app.py` / `ruff format --check` | PASS (extra checks) |
| `precedent preflight --offline` | PASS exit 0. Fixtures `READY` |

## What was not checked
- No live Anthropic investigate from the UI or CLI.
- Case Detail evidence/allocations/correction form (Session 15).
- Learning Results reports/activation (Session 16).
- Watchdog reload; Streamlit was restarted after UI edits.
- Held-out packages were not opened.

## Unresolved issues
- Live demo blocked until a real `ANTHROPIC_API_KEY`, a provider-verified
  `PRECEDENT_MODEL`, and `PRECEDENT_ENABLE_LIVE=true` exist. Then a UI Run
  on an OPEN case can be a live investigate. Do not substitute
  `ScriptedProvider` for that check.
- Git is not initialized; source version is a behavior-file hash.
- Do not run held-out until Session 18 after functional freeze.

## Deviations
- D001–D004 unchanged.
- Workspace control is `st.radio` (stable widget key). Case Detail / Learning
  Results are labeled stubs. AppTest patches live readiness only to prove
  that navigation does not repeat `investigate`; the patched call still uses
  TEST + `ScriptedProvider` inside the test, not as the UI live path.

## Next-session prerequisites
- Activate `.venv` (`source .venv/bin/activate`).
- Read spec Session 15 plus I3, G1, and D8.
- Build Case Detail, evidence inspection, and human correction only.
- Do not start Session 16, consume H01–H20, or treat a scripted AppTest run
  as a live investigate.

## Next chat prompt

```text
Implement Session 15 from docs/PRECEDENT_BUILD_SPEC.md
Read @AGENTS.md, @docs/BUILD_STATE.md, @docs/sessions/14.md, the reference
sections listed in Session 15 (I3; G1; D8), and the actual files you
will edit. Inspect the repository before assuming a prerequisite is done.
Implement only Session 15. Preserve the contracts and existing working
behavior. Run the UI correction flow on T03; inspect allocations and
correction after browser refresh. Add/run a meaningful UI test in
tests/test_app.py if AppTest supports the flow. Replace placeholder
IDs with actual generated IDs from the operator manifest. Use T03 IDs
(case CASE-1CFA9FEE848F, payment PAY-201, remittance DOC-R201, fee notice
DOC-F201, invoice INV-1042) and the saved teaching correction
COR-bd383d0aae4342c08c9c4170618b527f when still present. Update
docs/BUILD_STATE.md and write docs/sessions/15.md with results,
limitations, and the next-session prompt. If something is partially
complete, resume it; do not rebuild it from scratch. Do not start Session 16.
Live API remains unavailable until ANTHROPIC_API_KEY, PRECEDENT_MODEL, and
PRECEDENT_ENABLE_LIVE=true are configured; do not substitute mock behavior
for that live check. Do not consume H01–H20.
```
