# Session 20 - Ink/paper UI pass

Not a spec session. Presentation-layer pass on the D005 design layer: warm
paper tokens, type and fonts, custom-block alignment, and Streamlit chrome.
Investigator behavior files were not touched.

## Goal

Replace the Tailwind-slate / blue-800 / amber-100 look with ink/paper,
put custom-block text on a shared grid, restyle Streamlit chrome, and close
out per AGENTS.md with D008, an updated build state, and this log. Verify
with pytest at the 252 baseline, ruff, contrast on computed backgrounds, and
a browser pass at 1440px and 1024px.

## Completed changes

**Retoken.** Canvas `#F3EEE4`, surface `#FFFBF4`, recessed `#E8E1D4`, ink
`#1A1612`, muted `#5A544C`, faint `#6B6358`, navy `#1E3A5F`, amber `#8A5808`.
Dropped `NAVY_SOFT` / `INDIGO` / `INDIGO_SOFT` fills. State badges outlined
on paper except `NEEDS_REVIEW` (solid amber, white text). Unexplained
emphasis is a 3px amber left rule plus amber type on paper, not `#FEF3C7`.
Streamlit yellow/blue/green/red maps use paper fills. Radius 2px
(`0.125rem`).

**Type and fonts.** `.pc-section-title` is `1.375rem` / 600. Sidebar metric
value is `1.125rem`. Vendored static IBM Plex Sans latin 400/500/600; the
file previously named `IBMPlexSans-latin-400.woff2` was the variable latin
cut.

**Alignment.** Shared card inset `0.75rem 1rem`. Gap strip is three equal
columns with the amber rule inside the unexplained cell. Equation is a
baseline grid (`auto x 5`); operators sit on the money line (measured 1px).
Ident chips default to `20ch`. Recorded-run ledger is a bordered table
(Invoice / Opening outstanding / Closing outstanding).

**Chrome.** `toolbarMode=viewer` (Main menu is Print / Record screen / Copy
version; no Deploy). `disableDataExport=true`. Alerts are paper plus an
inset left rule. Dataframe element toolbar hidden. Evidence actions packed
left with `width="content"` so "Read full document" is not ellipsized.
Empty cash/fee is `theme.quiet`, not `st.error`; parse failures still error.

## Files changed

Edited `.streamlit/config.toml`, `ui_theme.py`, `ui.py`, `ui_case.py`,
`ui_results.py`; replaced Sans 400 and added Sans 500/600 woff2; docs D008,
BUILD_STATE, this file. Screenshots in `var/session-20-qa/` (gitignored).
Forbidden set untouched (`agent.py`, `tools.py`, `evidence.py`, `ledger.py`,
`policies.py`, `prompts/investigator.md`), plus `services.py` / `cli.py`.

## Commands and checks

| Check | Outcome |
|---|---|
| `.venv/bin/python -m pytest -q` | PASS, 252 passed in 8.10s |
| `.venv/bin/python -m ruff check .` | PASS |
| `.venv/bin/python -m ruff format --check .` | PASS, 69 files |
| Browser 1440px and 1024px, four pages | PASS. See below |
| Contrast, computed fg against walk-up bg | PASS. Text pairings clear 4.5:1. Tightest: faint on recessed 4.55:1 |

Browser QA ran on throwaway Streamlit port 8512 against the demo ledger.
The user's server on 8501 was left running and untouched.

1440: Work Queue; T03 Evidence / Decision / Teach; T06 evidence gap
("Nothing references ST-276395BFD4E3", paper plus amber rule, no yellow
slab); Lessons empty; Results recorded run `RUN-00ef3035d78e45e698a4356306bf548f`
with INV-1042 `$10,000.00` to `$0.00`.

1024: Work Queue (KPIs wrap; `$100.00` / `$35.00` untruncated); T03
Evidence / Decision / Teach; Lessons empty; Results ledger. T06 gap was
photographed at 1440; 1024 Case Detail used T03, which shares the same
gap-strip chrome.

## Contrast measurements

Faint `#6B6358` on recessed `#E8E1D4` 4.55:1 (tightest body); faint on
surface 5.73; muted on recessed 5.75; amber on surface 5.85 / recessed 4.64;
white on amber 6.03; navy on surface 11.15; ink on surface 17.44; Lessons
alert 17.44. Live Results: 87 elements / 18 variants, 0 failures. Hairline
`#D4CBBA` on surface is 1.56:1 (decorative). 3:1 applies to text, the 2px
navy focus ring, and badge outlines.

## Not checked

No live model call (`evaluate --live` not run; tests still monkeypatch
`LIVE UNAVAILABLE`). No screen-reader pass, no viewport below 1024px, no
dark mode. T06 gap not re-photographed at 1024.

## Unresolved issues

- `src/precedent/services.py` line 18 still says "for Learning Results".
- `precedent case show` still lists every invoice; the UI narrows in
  `ui_reconcile`. Moving that rule into `services.py` touches the backend.
- At 1024px the queue table still scrolls horizontally. The thesis columns
  stay visible; State and Latest result do not.

## Deviations

- D008 records the palette and chrome. D005-D007 stand. No spec requirement
  changed. Evidence columns used `[2.4, 3.2, 4.4]` plus `width="content"`
  because the plan's `[1, 1, 4]` ellipsized "Read full document".

## Next-session prerequisites

None required. Spec sessions 01-18 are complete. Presentation passes 19 and
20 are closed. Live work still needs `PRECEDENT_ENABLE_LIVE=true`, a real
`ANTHROPIC_API_KEY`, and a callable `PRECEDENT_MODEL`.

## Copy-paste prompt for the next chat

> Repository `/Users/timothy/Projects/HackMIT_project_26`. Read `AGENTS.md`,
> `docs/BUILD_STATE.md`, `docs/DECISIONS.md` (D001 to D008), and
> `docs/sessions/20.md` first. Spec sessions 01 to 18 are complete and the
> Session 19-20 presentation passes are closed; the suite is 252 passing with
> ruff clean. Do not start a new architecture and do not relabel a recorded
> or scripted run as LIVE.
>
> Pick one of these, and only one:
>
> (a) Live run, if `PRECEDENT_ENABLE_LIVE`, `ANTHROPIC_API_KEY`, and
> `PRECEDENT_MODEL` are set. Run `precedent preflight --live`, then
> `precedent evaluate --workspace WS-TEACH-001 --split heldout --live
> --deadline-seconds 5400`, then `precedent report show --id` with the
> printed experiment ID. Preserve `EXP-S18-HELDOUT-BLOCKED` as its own
> record; a later run is a new experiment ID. Do not substitute
> `ScriptedProvider`.
>
> (b) Remaining follow-ups: the stale "Learning Results" docstring in
> `services.py`, and optionally moving candidate-invoice narrowing from
> `ui_reconcile.py` into `services.py`. The last one touches the backend, so
> decide deliberately and record it in `docs/DECISIONS.md`.
>
> End with an updated `docs/BUILD_STATE.md` and `docs/sessions/21.md`.
