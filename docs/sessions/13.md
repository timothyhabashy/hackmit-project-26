# Session 13 — Reproducible evaluation runner and honest metrics

## Goal
Extend the isolated executor/scorer into a CLI paired evaluator that scores
baseline versus frozen approved memory, writes JSON and a readable report,
and keeps answer keys evaluator-only. Do not start Session 14 or consume
H01–H20.

## Completed changes
- `run_paired_evaluation` creates fresh `WS-EVAL-*` ledgers per case and arm
  from fixture packages. Baseline injects an empty snapshot; the memory arm
  freezes ACTIVE compatible lessons from the source workspace. Isolated
  copies do not mutate the source ledger.
- Scorer still loads oracles after the run. Known correct resolutions,
  incorrect automatic resolutions, and allowed reviews score as specified.
  Opening ledger fingerprints match across arms. A `call_cap` stop writes
  `PARTIAL` with completed pairs and explicit not-run rows, not filled zeros.
- Artifacts: `artifacts/evaluations/<experiment_id>/manifest.json`,
  `episodes.jsonl`, `report.json`, `report.md`, and bounded traces.
  `report.json` is the aggregate source. TEST vs LIVE is labeled. Missing
  usage/prices stay unavailable. No significance or labor-saving claim.
- CLI: `precedent evaluate --workspace ID --split teaching --limit 2 --live`
  and `precedent report show --id EXPERIMENT_ID`. `--live` never selects a
  scripted double. `--limit` is rejected for held-out.

T03 IDs still in `WS-TEACH-001`: case `CASE-1CFA9FEE848F`, payment `PAY-201`,
remittance `DOC-R201`, fee notice `DOC-F201`, invoice `INV-1042` outstanding
`1000000`. Teaching correction
`COR-bd383d0aae4342c08c9c4170618b527f` remains eligible and unapplied.
Teaching `--limit 2` is T01 `CASE-45E5749872CE` and T02 `CASE-D3F251969B6A`.

## Actual files changed
- `src/precedent/models.py`, `src/precedent/evaluation.py`, `src/precedent/db.py`
- `src/precedent/services.py`, `src/precedent/cli.py`
- `tests/test_evaluation.py`
- `README.md`, `.gitignore`, `docs/BUILD_STATE.md`, `docs/sessions/13.md`

## Commands and outcomes
All from repo root with `.venv` (CPython 3.11.14).

| Command | Outcome |
|---|---|
| `python -m pytest -q tests/test_evaluation.py` | PASS. 10 passed in 0.48s |
| Scripted teaching `--limit 2`; both arms correct; source ledger unchanged | PASS |
| Opening ledger fingerprints match across arms | PASS |
| `call_cap=1` partial report lists not-run, does not zero-fill | PASS |
| Oracle fields absent from traces/episode JSONL | PASS |
| Repeat test-double run reproduces scores; experiment IDs differ | PASS |
| Regression `tests/test_models.py` … `tests/test_evaluation.py` | PASS. 216 passed in 6.16s |
| `python -m ruff check src tests` / `ruff format --check` | PASS (extra checks) |
| `precedent preflight --offline` | PASS exit 0. Fixtures `READY` |
| `precedent evaluate --workspace WS-TEACH-001 --split teaching --limit 2 --live` | FAIL exit 2. `LIVE_DISABLED`. Scheduled 2 cases / 4 episodes printed. No artifacts, no EVAL workspaces, no HTTP |

## What was not checked
- No live Anthropic paired evaluation, compile, or activation.
- Scripted teaching smoke used an empty memory snapshot because the demo DB
  has no ACTIVE lesson. It does not prove live memory transfer.
- Held-out packages were not opened or scored.
- `precedent report show` was not exercised against a saved report because
  the live evaluate refused before writing artifacts.

## Unresolved issues
- Live demo blocked until a real `ANTHROPIC_API_KEY`, a provider-verified
  `PRECEDENT_MODEL`, and `PRECEDENT_ENABLE_LIVE=true` exist. Then compile,
  live-test, and activate a lesson before a live
  `precedent evaluate --workspace WS-TEACH-001 --split teaching --limit 2 --live`.
- Git is not initialized; source version is a behavior-file hash.
- Do not run held-out until Session 18 after functional freeze.

## Deviations
- D001–D004 unchanged.
- Evaluation reports add optional arm metrics, dispositions, and freeze
  fields on the Session 02 models. Required fields remain.

## Next-session prerequisites
- Activate `.venv` (`source .venv/bin/activate`).
- Read spec Session 14 plus I1–I2, I5, and F7.
- Build the Streamlit shell and Work Queue only.
- Do not start Session 15, consume H01–H20, or treat scripted scores as live.

## Next chat prompt

```text
Implement Session 14 from docs/PRECEDENT_BUILD_SPEC.md
Read @AGENTS.md, @docs/BUILD_STATE.md, @docs/sessions/13.md, the reference
sections listed in Session 14 (I1–I2; I5; F7), and the actual files you
will edit. Inspect the repository before assuming a prerequisite is done.
Implement only Session 14. Preserve the contracts and existing working
behavior. Run `streamlit run app.py --server.address 127.0.0.1` from the
existing `.venv` (Python 3.11.14). Open the queue, select a case, run it,
navigate away/back; verify run count does not increase. Replace placeholder
IDs with actual generated IDs from the operator manifest. Use T03 IDs
(case CASE-1CFA9FEE848F, payment PAY-201, remittance DOC-R201, fee notice
DOC-F201, invoice INV-1042) and the saved teaching correction
COR-bd383d0aae4342c08c9c4170618b527f when still present. Update
docs/BUILD_STATE.md and write docs/sessions/14.md with results,
limitations, and the next-session prompt. If something is partially
complete, resume it; do not rebuild it from scratch. Do not start Session 15.
Live API remains unavailable until ANTHROPIC_API_KEY, PRECEDENT_MODEL, and
PRECEDENT_ENABLE_LIVE=true are configured; do not substitute mock behavior
for that live check. Do not consume H01–H20.
```
