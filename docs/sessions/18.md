# Session 18 — Frozen held-out comparison, demo package, and final handoff

## Goal
Attempt the complete held-out paired comparison within the configured
API budget after the Session 17 freeze. If live credentials are missing,
record BLOCKED without substituting `ScriptedProvider`. Produce the
final report, concise README, three-minute demo script, limitations, and
submission paragraph. Do not tune product logic. Do not invent an effect.

## Completed changes
- Ran `precedent evaluate --workspace WS-TEACH-001 --split heldout --live
  --deadline-seconds 5400`. Exit 2, `LIVE_DISABLED`, before
  `load_split_suite`. No EVAL workspaces, no oracles, no `report.json`.
- Wrote blocked local record
  `artifacts/evaluations/EXP-S18-HELDOUT-BLOCKED/` (`status.json`,
  `report.md` only).
- Wrote `docs/RESULTS.md` (findings, L3 unused, L5 checklist, submission
  paragraph), `docs/DEMO_SCRIPT.md` (current disabled live controls),
  and a concise `README.md` (setup / demo / reset).
- T03 on `WS-TEACH-001` remains applied: `CASE-1CFA9FEE848F`, `PAY-201`,
  `INV-1042`, `DOC-R201`, `DOC-F201`,
  `COR-bd383d0aae4342c08c9c4170618b527f`,
  `APP-44b27f710fba4a67b8c06faff43b28d1`, HUMAN
  `RUN-00ef3035d78e45e698a4356306bf548f`. T06 and V02 remain OPEN.

## Actual files changed
- `README.md`
- `docs/RESULTS.md`, `docs/DEMO_SCRIPT.md`, `docs/BUILD_STATE.md`,
  `docs/sessions/18.md`
- `artifacts/evaluations/EXP-S18-HELDOUT-BLOCKED/status.json`,
  `artifacts/evaluations/EXP-S18-HELDOUT-BLOCKED/report.md`

No `_BEHAVIOR_FILES` edits. No evaluator or policy change.

## Commands and outcomes
All from repo root with `.venv` (CPython 3.11.14).

| Command | Outcome |
|---|---|
| `python -m pytest -q` | PASS. 252 passed in 6.26s |
| `python -m ruff check .` | PASS |
| `python -m ruff format --check .` | PASS. 54 files |
| `precedent preflight --offline` | PASS. Fixtures READY |
| `precedent preflight --live` | FAIL exit 2. `LIVE_DISABLED` |
| `precedent evaluate … --split heldout --live --deadline-seconds 5400` | FAIL exit 2. `LIVE_DISABLED`. Scheduled all split cases / 2 episodes |
| `precedent report show --id EXP-S18-HELDOUT-BLOCKED` | FAIL exit 2. report not found |
| `precedent cases list` / `case show` T03, T06, V02 | PASS. See BUILD_STATE |
| Browser `http://127.0.0.1:8501` Work Queue / T03 / Learning Results | PASS. LIVE unavailable; Open 9 / Resolved 1; T03 applied; Propose disabled naming live vars; RECORDED RUN; INV-1042 10,000.00 → 0.00; no saved `report.json`; T06 OPEN $9,965 in queue |

## What was not checked
- No live Anthropic investigate, lesson compile, ten-episode suite, or
  evaluation. Missing `ANTHROPIC_API_KEY`, `PRECEDENT_MODEL`,
  `PRECEDENT_ENABLE_LIVE=true`.
- Held-out source packages and grading files were not opened.
- Screen recording capture.
- V02 Case Detail was not re-opened in the browser this session (CLI
  `case show` on `WS-CAND-001` was).

## Unresolved issues
- Central live path BLOCKED until real credentials and
  `PRECEDENT_ENABLE_LIVE=true`. Then propose on T03, live-test the draft,
  inspect all ten episodes, activate only if that LIVE report passed,
  then held-out evaluate. Do not substitute `ScriptedProvider`.
- No precedent row / ACTIVE memory.
- Orphan `RUNNING` inspect runs in candidate DBs.
- Git is not initialized.
- A future live held-out run is a new experiment. Keep this BLOCKED
  record. Do not tune from those outcomes.

## Deviations
- D001–D004 unchanged.
- Required `report show` has no scored ID because evaluate refused
  before writing `report.json`. Documented as BLOCKED, not as zeros.
- L4 cuts remain unbuilt.

## Next-session prerequisites
Spec sessions are finished. If live credentials appear: activate
`.venv`, read `docs/RESULTS.md` and this file, run live preflight, then
the held-out command. Replace `EXPERIMENT_ID` with the printed ID.
Preserve `EXP-S18-HELDOUT-BLOCKED`.

## Next chat prompt

```text
Live credentials are now available for Precedent. Do not start a new
architecture. Read AGENTS.md, docs/BUILD_STATE.md, docs/sessions/18.md,
and docs/RESULTS.md. Keep the Session 18 BLOCKED record. Run
precedent preflight --live, then
precedent evaluate --workspace WS-TEACH-001 --split heldout --live
--deadline-seconds 5400. If a lesson must be compiled and
candidate-tested first, do that as LIVE only; do not use
ScriptedProvider. Do not tune product logic from held-out outcomes.
If interrupted, save PARTIAL with explicit not-run rows. Update
docs/RESULTS.md and docs/BUILD_STATE.md with the new experiment ID.
T03 remains CASE-1CFA9FEE848F / PAY-201 / INV-1042 / DOC-R201 /
DOC-F201 / APP-44b27f710fba4a67b8c06faff43b28d1 /
COR-bd383d0aae4342c08c9c4170618b527f /
RUN-00ef3035d78e45e698a4356306bf548f.
```
