# Session 01 — Repository, dependency setup, and immediate API preflight

## Goal
Create a minimal installable Python package, configuration loading, offline
and live preflight commands, tests, and the cross-session instruction files.
Do not implement domain models, ledger behavior, or a UI.

## Completed changes
- `src` layout package `precedent-hackmit` with CLI entry `precedent`.
- Settings loader: env > `.env` > defaults; positive integer budgets; absent
  token prices are unknown; live mode defaults off.
- `precedent preflight --offline` checks imports, versions, config, writable
  `var/`, and source readiness without network calls.
- `precedent preflight --live` refuses to call the provider unless live mode,
  a key, and a model are configured. It never prints secrets.
- Config/preflight tests covering defaults, overrides, invalid limits/rates,
  missing credentials, and disabled live mode.

## Actual files changed
- `pyproject.toml`, `requirements.lock.txt`, `.env.example`, `.gitignore`,
  `README.md`, `AGENTS.md`
- `src/precedent/__init__.py`, `src/precedent/config.py`, `src/precedent/cli.py`
- `tests/test_config.py`
- `docs/BUILD_STATE.md`, `docs/DECISIONS.md`, `docs/sessions/01.md`

Created locally, not committed: `.venv/`, `var/`, caches, egg-info.

## Commands and outcomes
All from repo root with `.venv` on 2026-09-19 17:34:10 UTC.

| Command | Outcome |
|---|---|
| `python3.11 -m venv .venv` then `pip install -e '.[dev]'` | PASS. Python 3.11.14; streamlit 1.64.0; pydantic 2.13.5; anthropic 1.7.0 |
| `python -m pip freeze --exclude-editable > requirements.lock.txt` | PASS. 52 pinned lines |
| `python -m pytest -q tests/test_config.py` | PASS. 27 passed in 0.33s |
| `python -m ruff check .` | PASS (extra check) |
| `python -m ruff format --check .` | PASS (extra check) |
| `precedent preflight --offline` | PASS exit 0. Fixtures `NOT_READY`. `api_key: absent`. `model: UNSET` |
| `precedent preflight --live` | FAIL exit 2. `LIVE_DISABLED`. No provider HTTP request |

Package import check: `import precedent, streamlit, pydantic, anthropic, dotenv` succeeded in 3.11.14.

## What was not checked
- No live Anthropic request (live disabled, key absent, model unset).
- No Session 02+ modules, Streamlit app, SQLite schema, or fixtures.
- Git is not initialized; no commit hash.
- Recreate-from-lock (`pip install -r requirements.lock.txt`) was not re-run
  after writing the lock file.

## Unresolved issues
- Live demo blocked until a real `ANTHROPIC_API_KEY`, a provider-verified
  `PRECEDENT_MODEL`, and `PRECEDENT_ENABLE_LIVE=true` exist. Then re-run
  `precedent preflight --live`.
- `data/source` does not exist yet (Session 03).

## Deviations
- D001: 3.11.14 instead of default 3.13.7.
- D002: python-dotenv + pydantic, not pydantic-settings.
- D003: lock via `pip freeze --exclude-editable`.

## Next-session prerequisites
- Activate `.venv` (`source .venv/bin/activate`).
- Read spec Session 02 plus D, E3–E5, G2, J4, P.
- Keep integer cents, workspace isolation, and no provider objects in domain
  models. Do not add a migration framework or UI.

## Next chat prompt

```text
Implement Session 02 from @docs/PRECEDENT_BUILD_SPEC.md.
Read @AGENTS.md, @docs/BUILD_STATE.md, @docs/sessions/01.md, the reference
sections listed in Session 02 (D; E3–E5; G2; J4; P), and the actual files
you will edit. Inspect the repository before assuming a prerequisite is done.
Implement only Session 02. Preserve the contracts and existing working behavior.
Run `python -m pytest -q tests/test_models.py tests/test_db.py` from the
existing `.venv` (Python 3.11.14). Update docs/BUILD_STATE.md and write
docs/sessions/02.md with results, limitations, and the next-session prompt.
If something is partially complete, resume it; do not rebuild it from scratch.
Do not start Session 03. Live API remains unavailable until ANTHROPIC_API_KEY,
PRECEDENT_MODEL, and PRECEDENT_ENABLE_LIVE=true are configured; do not substitute
mock behavior for that live check.
```
