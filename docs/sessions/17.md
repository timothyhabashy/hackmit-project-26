# Session 17 — End-to-end integration and failure handling

## Goal
Integration and robustness only: J1 remaining CLI, labeled recorded-run
export/view, honest LIVE/RECORDED/TEST badges, reset-safe artifacts, and
the teaching/candidate UI checklist. Freeze after required behavior. Do
not start Session 18 or consume H01–H20.

## Completed changes
- `export_recorded_run` writes `artifacts/recorded-runs/<run_id>.json` with
  opening vs final snapshots, hashes, and a redacted public trace. It never
  calls `apply_proposal`. Opening cash is reconstructed by reversing a
  non-seeded `APPLICATION_COMMITTED` allocation.
- CLI: `precedent cases list`, `case show`, `replay export`. Amounts print
  as integer-cent USD text.
- Learning Results recorded-run viewer: persistent warning
  `Recorded run — no live model calls`; opening/final balances; hashes;
  expander that viewing never applies money. Demo-reset caption says
  manifests and reports are kept.
- Header badges: `LIVE` / `LIVE unavailable` / `TEST SIMULATION` /
  `RECORDED RUN`. Learning Results with a saved manifest is `RECORDED RUN`
  even if the latest SQLite run is TEST.
- Tests: export survives `demo init`; CLI list/show/export; AppTest viewer
  does not apply T03.

T03 on `WS-TEACH-001` remains applied: `CASE-1CFA9FEE848F`, `PAY-201`,
`INV-1042`, `DOC-R201`, `DOC-F201`, `COR-bd383d0aae4342c08c9c4170618b527f`,
`APP-44b27f710fba4a67b8c06faff43b28d1`. HUMAN recorded apply
`RUN-00ef3035d78e45e698a4356306bf548f`. Isolated TEST capture
`RUN-S17-T03-REVIEW.json` (gitignored copy). No ACTIVE lesson.

## Actual files changed
- `src/precedent/services.py`, `src/precedent/cli.py`, `src/precedent/ui.py`
- `tests/test_services.py`, `tests/test_app.py`
- `README.md`, `docs/BUILD_STATE.md`, `docs/sessions/17.md`

Behavior fingerprint files were not edited. G4a was not required.

## Commands and outcomes
All from repo root with `.venv` (CPython 3.11.14).

| Command | Outcome |
|---|---|
| `python -m pytest -q` | PASS. 252 passed in 6.23s |
| `python -m ruff check .` | PASS |
| `python -m ruff format --check .` | PASS. 53 files |
| `precedent cases list --workspace WS-TEACH-001` | PASS. T03 RESOLVED; T06 OPEN $9,965 |
| `precedent case show` T03 | PASS. `INV-1042` $0; `DOC-R201`; `DOC-F201` |
| Recorded-run JSON secret scan | PASS. No `sk-ant-` / key values |
| Browser Work Queue / T03 / Learning Results / T06 / V02 | PASS. See BUILD_STATE |
| Duplicate Learning Results + refresh | PASS. T03 still applied; Open 9 / Resolved 1; no Anthropic URLs |

Failure-path coverage (existing tests, not re-demoed live): missing
credentials, provider timeout, malformed tool output, empty search,
mismatched currency, missing evidence, stale proposal, rollback.

## What was not checked
- No live Anthropic investigate, lesson compile, ten-episode suite, or
  evaluation. Missing `ANTHROPIC_API_KEY`, `PRECEDENT_MODEL`,
  `PRECEDENT_ENABLE_LIVE=true`.
- Held-out packages were not opened.
- Watchdog reload; Streamlit served `http://127.0.0.1:8501` after restart.
- Browser did not click New demo workspace (would create another teaching
  ledger). Reset-survival is covered by pytest.

## Unresolved issues
- Live path BLOCKED until real credentials and `PRECEDENT_ENABLE_LIVE=true`.
  Then Propose on T03, live-test the draft, inspect all ten episodes, and
  activate only if that LIVE report passed. Do not substitute
  `ScriptedProvider`.
- No precedent row / ACTIVE memory in `var/precedent.sqlite3`.
- Orphan `RUNNING` inspect runs in candidate DBs (`RUN-INSPECT-V02-FOLLOW`).
- Git is not initialized; source version is a behavior-file hash.
- Do not run held-out until Session 18 after this freeze.

## Deviations
- D001–D004 unchanged.
- Scope is frozen: no new service, provider, or feature expansion before
  Session 18. L4 cuts (custom CSS, extra exporters) remain unbuilt.
- `case show` still lists all workspace invoices (pre-existing).

## Next-session prerequisites
- Activate `.venv` (`source .venv/bin/activate`).
- Read spec Session 18 plus H6–H8 and L1–L5.
- Run held-out only with `--live` after credentials exist; otherwise record
  BLOCKED honestly. Do not relabel a teaching `--limit` run as the complete
  experiment.
- Do not consume H01–H20 for product tuning.

## Next chat prompt

```text
Implement Session 18 from docs/PRECEDENT_BUILD_SPEC.md
Read @AGENTS.md, @docs/BUILD_STATE.md, @docs/sessions/17.md, the reference
sections listed in Session 18 (H6–H8; L1–L5), and the actual files you
will edit. Inspect the repository before assuming a prerequisite is done.
Implement only Session 18. Preserve the contracts and existing working
behavior. Run the complete held-out comparison within the configured API
budget if live credentials exist; otherwise record BLOCKED and do not
substitute ScriptedProvider. If interrupted, save honest partial outcomes.
Produce the final report, a concise README with setup/demo/reset steps, a
three-minute demonstration script, a limitations section, and a
submission-ready paragraph. Use T03 IDs (case CASE-1CFA9FEE848F, payment
PAY-201, remittance DOC-R201, fee notice DOC-F201, invoice INV-1042),
application APP-44b27f710fba4a67b8c06faff43b28d1, teaching correction
COR-bd383d0aae4342c08c9c4170618b527f, recorded HUMAN run
RUN-00ef3035d78e45e698a4356306bf548f, V02 CASE-84F1300FE6B4, and T06
CASE-D19F18830478. Update docs/BUILD_STATE.md and write docs/sessions/18.md.
If something is partially complete, resume it; do not rebuild it from
scratch. Live API remains unavailable until ANTHROPIC_API_KEY,
PRECEDENT_MODEL, and PRECEDENT_ENABLE_LIVE=true are configured. Do not
tune product logic from held-out outcomes. Do not invent an effect.
```
