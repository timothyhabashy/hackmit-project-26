# Session 12 — Retrieve learned memory and prove persistence

## Goal
Load approved persisted lessons for ordinary retrieval, freeze them at run
start from `workspace_memory`, and prove the snapshot survives close/reopen.
Do not start Session 13 or consume H01–H20.

## Completed changes
- Ordinary `memory_mode=on` freezes ACTIVE compatible lessons attached in the
  current workspace. Company, policy, status, and behavior fingerprint
  mismatches are excluded with a stored reason. `memory_mode=off` still uses
  an empty snapshot through the same `retrieve_precedents` tool.
- Evaluator injection is unchanged: an explicit `memory_snapshot` wins, and
  candidate tests keep `memory_mode=off` so they do not load memberships.
- `retrieve_precedents` still requires an opened payment-bound remittance,
  returns at most three in-scope hints with version, scope, and lookup, and
  records considered/excluded reasons on `precedent_retrieved`. A lesson is a
  hint; it does not apply money or skip the validator.
- `demo new --dataset candidate --memory-from SOURCE` copies the source
  workspace's currently ACTIVE compatible memberships. Default `demo new`
  attaches none. Retirement leaves past traces and drops future retrieval.
- Persistence tests close and reopen SQLite, then inspect V01–V04 in a
  February follow-up workspace. V01 retrieves the Harbor lesson; V02/V04
  retrieve it but do not post unsupported fees; V03 (Cedar) does not retrieve
  it; a default workspace and `memory off` return none.

T03 IDs still in `WS-TEACH-001`: case `CASE-1CFA9FEE848F`, payment `PAY-201`,
remittance `DOC-R201`, fee notice `DOC-F201`, invoice `INV-1042` outstanding
`1000000`. Teaching correction
`COR-bd383d0aae4342c08c9c4170618b527f` remains eligible and unapplied.
Demo follow-up `WS-CAND-001` and default `WS-CAND-002` exist with 0 attached
lessons because the demo DB has no ACTIVE row.

## Actual files changed
- `src/precedent/models.py`, `src/precedent/learning.py`, `src/precedent/agent.py`
- `src/precedent/tools.py`, `src/precedent/services.py`, `src/precedent/evaluation.py`
- `tests/test_learning.py`, `tests/test_tools.py`
- `README.md`, `docs/BUILD_STATE.md`, `docs/sessions/12.md`

## Commands and outcomes
All from repo root with `.venv` (CPython 3.11.14).

| Command | Outcome |
|---|---|
| `python -m pytest -q tests/test_learning.py tests/test_tools.py` | PASS. 38 passed in 2.34s |
| Close/reopen in pytest; V01/V02 follow-up with attached ACTIVE memory | PASS. Lesson retrieved; V02 no fee application |
| Default new workspace and `memory off` retrieve none | PASS |
| Retired lesson excluded after reopen; past V01 trace kept | PASS |
| Incompatible behavior fingerprint excluded with a visible reason | PASS |
| Regression `tests/test_models.py` … `tests/test_evaluation.py` | PASS. 210 passed in 5.70s |
| `python -m ruff check src tests` / `ruff format --check` | PASS (extra checks) |
| `precedent preflight --offline` | PASS exit 0. Fixtures `READY` |
| `precedent demo new --dataset candidate --memory-from WS-TEACH-001` | PASS. `WS-CAND-001`, attached 0 |
| `precedent demo new --dataset candidate` | PASS. `WS-CAND-002`, attached 0 |
| Reopen demo DB; inspect V01 `CASE-9A654576715E` and V02 `CASE-84F1300FE6B4` | PASS. Both workspaces returned no hints |

## What was not checked
- No live Anthropic compile, ten-episode live suite, or live activation.
  Scripted persistence checks used a bound LIVE copy of a TEST report and are
  not a live verification.
- Demo DB retrieve inspections were TEST `dispatch_tool` reads after opening
  the current remittance. They were not live `precedent run --live` episodes
  and did not claim the lesson caused a better investigation.
- Held-out packages were not opened or used to tune retrieval.
- No call-count improvement was forced or claimed.

## Unresolved issues
- Live demo blocked until a real `ANTHROPIC_API_KEY`, a provider-verified
  `PRECEDENT_MODEL`, and `PRECEDENT_ENABLE_LIVE=true` exist. Then
  `precedent lesson propose --correction COR-bd383d0aae4342c08c9c4170618b527f --live`,
  inspect the draft, `precedent lesson test --id PRECEDENT_ID --live`, inspect
  all ten episodes, activate only if that LIVE report passed, then recreate a
  February follow-up with `--memory-from WS-TEACH-001` and inspect V01/V02
  against the current behavior fingerprint.
- `agent.py` and `tools.py` changed, so the behavior fingerprint changed.
  There was no ACTIVE lesson to redraft.
- Git is not initialized; no commit hash.

## Deviations
- D001–D004 unchanged.
- Demo follow-up workspaces were created with zero attached lessons. That
  matches G5/I6 when the source has no ACTIVE memberships; it is not persisted
  live reuse.

## Next-session prerequisites
- Activate `.venv` (`source .venv/bin/activate`).
- Read spec Session 13 plus H5–H8, J3–J4, and P.
- Extend Session 11's isolated executor/scorer into a CLI paired evaluator.
- Do not start Session 14, consume H01–H20, or treat scripted scores as live.

## Next chat prompt

```text
Implement Session 13 from docs/PRECEDENT_BUILD_SPEC.md
Read @AGENTS.md, @docs/BUILD_STATE.md, @docs/sessions/12.md, the reference
sections listed in Session 13 (H5–H8; J3–J4; P), and the actual files you
will edit. Inspect the repository before assuming a prerequisite is done.
Implement only Session 13. Preserve the contracts and existing working
behavior. Run `python -m pytest -q tests/test_evaluation.py` from the
existing `.venv` (Python 3.11.14). Run
`precedent evaluate --workspace WORKSPACE_ID --split teaching --limit 2 --live`
on isolated copies; do not run heldout yet. Replace placeholder IDs with
actual generated IDs from the operator manifest. Use T03 IDs
(case CASE-1CFA9FEE848F, payment PAY-201, remittance DOC-R201, fee notice
DOC-F201, invoice INV-1042) and the saved teaching correction
COR-bd383d0aae4342c08c9c4170618b527f when still present. Update
docs/BUILD_STATE.md and write docs/sessions/13.md with results,
limitations, and the next-session prompt. If something is partially
complete, resume it; do not rebuild it from scratch. Do not start Session 14.
Live API remains unavailable until ANTHROPIC_API_KEY, PRECEDENT_MODEL, and
PRECEDENT_ENABLE_LIVE=true are configured; do not substitute mock behavior
for that live check. Do not consume H01–H20.
```
