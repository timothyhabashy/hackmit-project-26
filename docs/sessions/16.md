# Session 16 — Learning results and before/after evidence

## Goal
Complete Learning Results: lesson version, original correction, candidate
test rows, explicit activation, and saved evaluation display. Do not start
Session 17 or consume H01–H20.

## Completed changes
- Learning Results replaces the Session 14/15 stub. It lists owned lessons
  for the remembered workspace, shows status/scope/lookup procedure,
  original correction, payload hash prefix, and the bound candidate report.
- Lifecycle buttons call existing services: `test_lesson` (LIVE, 10
  episodes), `activate_lesson`, `reject_lesson`, `retire_lesson`, and
  `redraft_lesson` as Create new version. Activation stays disabled until
  `activation_blockers` is empty. A draft cannot appear ACTIVE.
- Candidate rows expand baseline/candidate outcomes, reviewer expected
  disposition (oracle values stay off the runtime agent), validator
  rejections, traces, and V02 short-payment sources.
- Saved evaluations: `Load report` reads `report.json` and never starts an
  experiment. The metrics table is compared to that JSON. Incomplete counts
  are shown, not zero-filled. Teaching `--limit 2` is LIVE-only and names
  the budget (4 episodes, 5400s deadline).
- `New demo workspace` and candidate follow-up call `demo_new`. Recorded-run
  viewing is empty unless captured files exist; none were fabricated.

T03 in `WS-TEACH-001` remains applied: case `CASE-1CFA9FEE848F`, payment
`PAY-201`, invoice `INV-1042`, remittance `DOC-R201`, fee notice
`DOC-F201`, correction `COR-bd383d0aae4342c08c9c4170618b527f`, application
`APP-44b27f710fba4a67b8c06faff43b28d1`. No draft/ACTIVE lesson in the demo
DB. V02 is `CASE-84F1300FE6B4`.

## Actual files changed
- `src/precedent/ui.py`, `src/precedent/services.py`, `src/precedent/db.py`
- `tests/test_app.py`
- `README.md`, `docs/BUILD_STATE.md`, `docs/sessions/16.md`

## Commands and outcomes
All from repo root with `.venv` (CPython 3.11.14).

| Command | Outcome |
|---|---|
| AppTest: V02 rows, activate PASSING+LIVE-bound version, Load report vs JSON, rerun | PASS. Activate once; test/eval counts stayed 0 |
| Regression including `tests/test_app.py` | PASS. 222 passed in 5.77s |
| `python -m ruff check src tests app.py` / `ruff format --check` | PASS |
| `precedent preflight --offline` | PASS exit 0. Fixtures `READY` |
| `streamlit run app.py --server.address 127.0.0.1` | PASS. `http://127.0.0.1:8502` |
| Browser: Learning Results on `WS-TEACH-001` | PASS. No owned lessons; live eval disabled; three vars named |
| Browser refresh resource hosts | PASS. `127.0.0.1:8502` only for the app; no Anthropic URLs |
| Browser Work Queue after Learning Results | PASS. Open 9, Resolved 1 |

## What was not checked
- No live Anthropic compile, ten-episode live suite, or live evaluation.
- Demo DB has no draft, so browser activation used the empty state. AppTest
  bound a LIVE copy of a scripted PASSED report to exercise activation.
- Watchdog reload; Streamlit was restarted after UI edits.
- Held-out packages were not opened.

## Unresolved issues
- Live demo blocked until a real `ANTHROPIC_API_KEY`, a provider-verified
  `PRECEDENT_MODEL`, and `PRECEDENT_ENABLE_LIVE=true` exist. Then Propose on
  T03, live-test the draft, inspect all ten episodes, and activate only if
  that LIVE report passed. Do not substitute `ScriptedProvider`.
- No precedent row / ACTIVE memory in `var/precedent.sqlite3`.
- Git is not initialized; source version is a behavior-file hash.
- Do not run held-out until Session 18 after functional freeze.

## Deviations
- D001–D004 unchanged.
- AppTest activation uses the Session 11 pattern: a LIVE-labeled copy of a
  scripted PASSED report. That is not a live verification.

## Next-session prerequisites
- Activate `.venv` (`source .venv/bin/activate`).
- Read spec Session 17 plus J2–J5 and L1–L4.
- Integration/failure handling only. Freeze after required behavior works.
- Do not start Session 18, consume H01–H20, or treat a scripted suite as live.

## Next chat prompt

```text
Implement Session 17 from docs/PRECEDENT_BUILD_SPEC.md
Read @AGENTS.md, @docs/BUILD_STATE.md, @docs/sessions/16.md, the reference
sections listed in Session 17 (J2–J5; L1–L4), and the actual files you
will edit. Inspect the repository before assuming a prerequisite is done.
Implement only Session 17. Preserve the contracts and existing working
behavior. Run python -m pytest -q; python -m ruff check .; python -m ruff
format --check .; complete the manual UI/demo checklist on teaching and
candidate copies only. Replace placeholder IDs with actual generated IDs
from the operator manifest. Use T03 IDs (case CASE-1CFA9FEE848F, payment
PAY-201, remittance DOC-R201, fee notice DOC-F201, invoice INV-1042),
application APP-44b27f710fba4a67b8c06faff43b28d1, and teaching correction
COR-bd383d0aae4342c08c9c4170618b527f. Update docs/BUILD_STATE.md and write
docs/sessions/17.md with results, limitations, and the next-session prompt.
If something is partially complete, resume it; do not rebuild it from
scratch. Do not start Session 18. Live API remains unavailable until
ANTHROPIC_API_KEY, PRECEDENT_MODEL, and PRECEDENT_ENABLE_LIVE=true are
configured; do not substitute mock behavior for that live check. Do not
consume H01–H20.
```
